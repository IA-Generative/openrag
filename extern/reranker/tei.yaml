# Compose include for RERANKER_PROVIDER=tei.
#
# docker-compose.yaml includes extern/reranker/${RERANKER_PROVIDER}.yaml, and the
# application supports the `tei` provider (a Text Embeddings Inference `/rerank`
# endpoint), but only infinity.yaml and openai.yaml were shipped: with
# RERANKER_PROVIDER=tei every `docker compose` command failed on the missing file.
#
# A TEI reranker is reached over HTTP at RERANKER_BASE_URL, typically an external
# gateway, so there is no TEI-specific container to define. The services below are
# the same as openai.yaml: they must exist because deployments that use a remote
# reranker disable `reranker-gpu` / `reranker-cpu` by name (`scale: 0`).
x-vllm-env: &vllm_env
  HUGGING_FACE_HUB_TOKEN:
  VLLM_SLEEP_WHEN_IDLE: 1  # Avoid 100% CPU usage when idle

x-reranker: &reranker_template
  networks:
    default:
      aliases:
        - reranker
  # restart: on-failure
  environment:
    - HUGGING_FACE_HUB_TOKEN
    - VLLM_SLEEP_WHEN_IDLE=1  # Avoid 100% CPU usage when idle
  ipc: "host"
  volumes:
    - ${VLLM_CACHE:-/root/.cache/huggingface}:/root/.cache/huggingface
  command: >
    --model ${RERANKER_MODEL:-BAAI/bge-reranker-v2-m3}
    --trust-remote-code
    --api-key ${RERANKER_API_KEY:-"EMPTY"}
    --gpu_memory_utilization 0.3
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8000/health"]
    interval: 20s
    timeout: 5s
    retries: 4
    start_period: 90s
  # ports:
  #     - ${RERANKER_PORT:-8000}:8000

services:
  reranker-gpu:
    <<: *reranker_template
    image: vllm/vllm-openai:v0.17.1
    environment:
      <<: *vllm_env
      NVIDIA_VISIBLE_DEVICES: all
      NVIDIA_DRIVER_CAPABILITIES: compute,utility
    runtime: nvidia
    profiles:
      - ""
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu]

  reranker-cpu:
    <<: *reranker_template
    image: vllm/vllm-openai-cpu:v0.17.1
    deploy: {}
    environment:
      <<: *vllm_env
      VLLM_CPU_KVCACHE_SPACE: 8
    command: >
      --model ${RERANKER_MODEL:-BAAI/bge-reranker-v2-m3}
      --trust-remote-code
      --api-key ${RERANKER_API_KEY:-"EMPTY"}
      --dtype float32
    profiles:
      - "cpu"
